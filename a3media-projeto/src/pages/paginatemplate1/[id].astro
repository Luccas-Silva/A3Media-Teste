---
export const getStaticPaths = async () => {
  return Array.from({ length: 1000 }, (_, i) => ({
    params: { id: (i + 1).toString() },
    props: { pageNumber: i + 1 }
  }));
};

const { pageNumber } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página Template 1 - {pageNumber}</title>
  <style>
    /* Cole aqui o CSS da página https://getflowempower.com/ps/pscollins01 */
    /* ... seus estilos ... */
  </style>
</head>
<body>
  <main>
    {/* Cole aqui o HTML da página https://getflowempower.com/ps/pscollins01 */}
    <h1>Conteúdo da Página Template 1 - Número: {pageNumber}</h1>
    <div class="hero">
      {/* ... elementos do hero ... */}
    </div>
    <div class="main-content">
      {/* ... restante do conteúdo ... */}
    </div>
  </main>
</body>
</html>